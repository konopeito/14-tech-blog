<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        {{> post-info post }}
    </li>
    {{!-- When no posts are visible --}}
{{else}}
<h6 class="pb-4 has-text-centered is-italic">There are no available posts, try refreshing the page or making a post. </h6>
    {{/each}}
</ol>